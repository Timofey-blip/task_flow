# TaskFlow

Гем для описания и управления workflow (процессами) в Ruby.
## Callbacks

TaskFlow поддерживает колбэки, позволяющие выполнять произвольный код до и после смены состояний.

## Установка

Добавь в Gemfile:

```ruby
gem 'task_flow', git: 'https://github.com/Timofey-blip/task_flow.git'
